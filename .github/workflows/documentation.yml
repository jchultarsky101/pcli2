name: Deploy Documentation

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Setup mdBook
        uses: peaceiris/actions-mdbook@v1
        with:
          mdbook-version: 'latest'

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Oranda
        run: |
          curl --proto '=https' --tlsv1.2 -LsSf https://github.com/axodotdev/oranda/releases/latest/download/oranda-installer.sh | sh

      - name: Copy documentation files to ensure proper structure
        run: |
          # Ensure docs/src directory exists and has proper structure
          mkdir -p docs/src
          # Copy all documentation files to docs/src if they're not already there
          for file in docs/*.md; do
            if [ -f "$file" ]; then
              filename=$(basename "$file")
              if [ ! -f "docs/src/$filename" ]; then
                cp "$file" "docs/src/$filename"
              fi
            fi
          done
          
      - name: Build Documentation
        run: oranda build
      
      - name: Copy LICENSE file to documentation
        run: cp LICENSE public/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4